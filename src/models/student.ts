export interface Student {
    student_id: number;
    prefix: string;
    first_name: string;
    last_name: string;
}

export interface UniversityData {
    university_id: number;
    code: string;
    name: string;
    degree: string;
}