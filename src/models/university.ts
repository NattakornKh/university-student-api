export interface University {
    university_id: number;
    code: string;
    name: string;
}

export interface StudentData {
    student_id: number;
    prefix: string;
    first_name: string;
    last_name: string;
    degree: string;
}